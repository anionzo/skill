# OpenCode Adapter

Target file in a project repository: `OPENCODE.md`

Source artifact from this library: `generated/OPENCODE.md`

Use this when you want OpenCode to load a short platform-specific instruction file that points back to the real skill library.
